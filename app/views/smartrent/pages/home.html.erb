<div class="banner clearfix">
  <div class="fixedWidth">
    <div class="images">
      <div class="left">
        <%= image_tag "homePageLeft.jpg" %>
      </div>

      <div class="right">
        <%= image_tag "homePage.jpg" %>
      </div>
    </div>
  </div>
</div>

<section id="contentWrapper">
  <div class="fixedWidth">
    <div id="homeContent">
      When you rent from a Bozzuto SmartRent apartment community, you earn a
      $350 credit every month - up to a total of $10,000 - to help you
      purchase a new <%= link_to "condominium" ,new_home_path  %>,
      <%= link_to "townhome", new_home_path %> or
      <%= link_to "single-family home", new_home_path %>
       from <%= link_to "Bozzuto Homes", new_home_path %>
      Simple as that. No gimmicks. Best of all, no cost to you, whether you
      choose to participate in SmartRent or not.
      <%= link_to "Click here to learn more.", faq_path  %>
    </div>
    <div id="slides">
      <div class="slides_container">
          <% i = 0 %>
        <% Smartrent::Property.all.each do |property|%>
          <% if i%2 == 0 %>
          <div class="threeWide">
          <% end %>
            <div class="propertySlide">
              <%= link_to properties_path(property) do %>
                <%= image_tag property.left_image %>
                <div class="caption">
                  <strong><%= property.title %></strong> In every way, <%= property.title %> is truly the place to be.
                </div>
              <% end %>
            </div>
          <% i+=1 %>
          <% if i%2 == 0 %>
          </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</section>
