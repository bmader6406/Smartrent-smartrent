$('.listing-results').html('<%= j( render partial: "search_results" ) %>')
$('.map-results').html('');

if($('#mapResults.mapOn')[0]){
  Property.loadMap();
}

$('.property-img').imageTooltip({
  imgWidth: 580
});

$.unblockUI();